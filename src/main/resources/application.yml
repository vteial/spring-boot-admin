info:
  app:
    name: wss
    description: Wybis System Service
    version: 1.0.0

spring:
  application:
    name: Wybis System Service
  profiles:
    active: secure
  security:
    user:
      name: "sadmin"
      password: "wybis4321"
  boot:
    admin:
      client:
        url: http://localhost:8080
        username: ${spring.security.user.name}       #These two are needed so that the client
        password: ${spring.security.user.password}   #can register at the protected server api
        instance:
          service-base-url: http://localhost:8080
          metadata:
            user.name: ${spring.security.user.name}         #These two are needed so that the server
            user.password: ${spring.security.user.password} #can access the protected client endpoints

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS

---
spring:
  profiles: insecure
---

