info:
  app:
    name: sbas
    description: Spring Boot Admin Service
    version: 1.0.0

server:
  port: 8080
  servlet:
    context-path: /sbas

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS
#  server:
#    servlet:
#      context-path: /sbas

spring:
  application:
    name: Spring Boot Admin Service
  boot:
    admin:
      client:
        url: http://localhost:8080/sbas
        instance:
          service-base-url: http://localhost:8080/sbas
  profiles:
    active: secure

---
spring:
  profiles: insecure

---
spring:
  profiles: secure
  security:
    user:
      name: "sbadmin"
      password: "sbas4321"
  boot:
    admin:
      client:
        username: ${spring.security.user.name}      #These two are needed so that the client
        password: ${spring.security.user.password}  #can register at the protected server api
        instance:
          metadata:
            user.name: ${spring.security.user.name}         #These two are needed so that the server
            user.password: ${spring.security.user.password} #can access the protected client endpoints

